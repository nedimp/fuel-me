{
  "name": "fuel-me-interview-prep",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter site-manager dev\" \"pnpm --filter dispatcher dev\"",
    "build": "pnpm --filter shared build && pnpm --filter backend build && pnpm --filter site-manager build && pnpm --filter dispatcher build",
    "lint": "pnpm -r lint",
    "db:migrate": "pnpm --filter backend db:migrate",
    "db:studio": "pnpm --filter backend db:studio"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
